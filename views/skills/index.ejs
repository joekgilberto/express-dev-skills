<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>
    <ul>
        <% skills.forEach((skill)=>{ %>
            <li>
                <a href="/skills/<%= skill.id %>"><%= skill.skill %></a>
                <br />
                <em><%= skill.mastered ? "Mastered!" : "Learning!" %></em>
            </li>
        <% }) %>
    </ul>
</body>
</html>